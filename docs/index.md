# Welcome to the Stacks docs

``` lua title="wow"
print(test) -- (1)!
```

1. An annotation!

``` mermaid
graph LR
  A[Start] --> B{Error?};
  B -->|Yes| C[Hmm...];
  C --> D[Debug];
  D --> B;
  B ---->|No| E[Yay!];
```
